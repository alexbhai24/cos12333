import React from 'react';

// Admin Console has been consolidated into the full-screen page at /admin-dashboard
// This stub exists only to avoid breaking any lingering imports in the codebase
export const AdminConsoleModal: React.FC = () => null;
